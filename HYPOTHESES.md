# Лог гипотез

Каждая гипотеза, которую тестируем — сюда, с результатом (в т.ч.
отрицательным — см. PLAN.md, договорённость честно фиксировать провалы).
Перед тем как предлагать новую идею — сверяемся с этим списком, чтобы не
проверять одно и то же дважды.

Статусы: `proposed` (сформулирована, ещё не проверена) → `testing` →
`confirmed` / `rejected` / `inconclusive` (данных недостаточно для вывода).

---

## H1 — Экстремальный funding rate → откат цены

**Статус**: proposed (2026-09-07)

**Формулировка**: когда funding rate находится на экстремуме относительно
своей 90-дневной истории (перцентиль > 95 — толпа перегружена лонгами,
либо < 5 — перегружена шортами), в ближайшие N дней цена с повышенной
вероятностью и амплитудой движется ПРОТИВ перегруженной стороны.

**Данные**: `funding_percentile_90d` (таблица `indicators`) + цена
(`candles`). Полная история 2019-2026.

**Метод проверки**:
- Найти все дни с перцентилем > 0.95 или < 0.05
- Посчитать фактическую доходность цены за следующие N дней (не знак —
  число), сравнить с безусловной (случайной) доходностью за тот же период
- Учесть funding, уплаченный/полученный за время удержания позиции
  (реальная стоимость сделки, не только направление)
- Walk-forward по всей истории (не один разрез) — несколько независимых
  проверок на разных участках (бычьих/медвежьих)

**Результат**: —  (ещё не проверено)

**Вывод**: —

---
